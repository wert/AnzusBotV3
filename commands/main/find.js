const { ConLog } = require("../../kcso/convictionlog");

module.exports = {
    config: {
        name: "a",
        aliases: [],
        usage: "(command)",
        category: "staff",
        description: "",
        accessableby: "Staff",
        guilds: ["532966690695151625"],
        channels: []
    },
    run: async (bot, message, args) => {
        let search = args[0];
    }
};